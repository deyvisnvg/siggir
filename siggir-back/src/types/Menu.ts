export interface MenuData {
    menuId: number;
    menuNombre: string;
    menuUrl: string;
    menuEstado: string;
}

export interface MenuBody {
    menuNombre: string;
    menuUrl: string;
    menuEstado: string;
}